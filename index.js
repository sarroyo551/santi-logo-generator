const inquirer = require('inquirer')
const shapes = require('./lib/shapes')

inquirer
  .prompt([
  ])
  .then((answers) => {
  })
  .catch((error) => {
    if (error.isTtyError) {
    } else {
    }
  });